services:
  cyclic-example-db:
    image: postgres:latest
    environment:
      POSTGRES_PASSWORD: secret
    depends_on:
      - web
  cyclic-example-web:
    image: nginx:latest
    ports:
      - "8080:80"
    depends_on:
      - db