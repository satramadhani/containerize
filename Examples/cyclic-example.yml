services:
  db:
    image: postgres:latest
    environment:
      POSTGRES_PASSWORD: secret
    depends_on:
      - web
  web:
    image: nginx:latest
    ports:
      - "8080:80"
    depends_on:
      - db