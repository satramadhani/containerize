services:
  db:
    image: postgres:latest
    environment:
      POSTGRES_PASSWORD: secret
    volumes:
      - ./data:/var/lib/postgresql/data
    depends_on:
      - web
  web:
    image: nginx:latest
    ports:
      - "8080:80"